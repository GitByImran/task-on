"use client";

import { ChevronsLeftRight } from "lucide-react";
import React from "react";

const Expander = () => {
  const handleShowLabel = () => {};
  return (
    <div className="" onClick={handleShowLabel}>
      <ChevronsLeftRight />
    </div>
  );
};

export default Expander;
